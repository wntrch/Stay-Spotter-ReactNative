import { ScrollView, Text } from "react-native";



const Contact = () => {
    return (
        <ScrollView>
            <Text>Contact Page</Text>
        </ScrollView>

    );
};

export default Contact;